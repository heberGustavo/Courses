import MostrarTituloExemplo from "../../utils/utils";
import { div, mult, somar, sub } from "./math";

MostrarTituloExemplo("Exemplo 01");

console.log(`Soma: ${somar(12, 38)}`);
console.log(`Sub: ${sub(10, 38)}`);
console.log(`Div: ${div(10, 10)}`);
console.log(`Mult: ${mult(3, 100)}`);
