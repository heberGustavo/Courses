{
  "compilerOptions": {
    "target": "ES2022",                           //Define qual versão do JavaScript será gerada no final.
    "module": "ESNext",                           //Define o formato de módulo: ESNext permite usar import/export nativos.
    "outDir": "./dist",                           //Pasta onde o TypeScript vai colocar os arquivos compilados (JS).
    "rootDir": "./src",                           //Pasta raiz onde está seu código TypeScript fonte.
    "strict": true,                               //Ativa strict mode: garante tipagem 100% segura.
    "esModuleInterop": true,                      //Permite importar módulos CommonJS (como import express from 'express') sem erro, mesmo que sejam de estilo antigo. Necessário para integração com bibliotecas JS externas.
    "moduleResolution": "node",                   //Diz ao compilador para resolver módulos como o Node.js faz: Busca em node_modules Suporta index.ts, index.js, etc. Essencial para Angular e qualquer projeto moderno.
    "forceConsistentCasingInFileNames": true,     //Garante que você não use letras maiúsculas/minúsculas de forma inconsistente em nomes de arquivos e imports.
    "skipLibCheck": true                          //Pula a verificação de tipos em arquivos .d.ts de dependências. Acelera o build e evita erros chatos vindos de bibliotecas de terceiros. Recomendado para apps; em libs, geralmente se deixa como false.
  },
  "include": ["src"],                             //Diz ao TypeScript: “compile tudo que está dentro da pasta src”.
  "exclude": ["node_modules", "dist"]             //Ignora essas pastas no processo de build. 'node_modules' não precisa ser compilado. 'dist' é onde cai o JS, então evitar compilar de volta previne loops infinitos.
}
